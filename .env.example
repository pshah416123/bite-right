# Optional: set when testing on a physical device so the app can reach the backend.
# Use your computer's IP (find in System Settings → Network). Example:
# EXPO_PUBLIC_API_URL=http://192.168.1.5:4000
